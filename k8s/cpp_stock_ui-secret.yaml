apiVersion: v1
kind: Secret
metadata:
  name: cpp-stock-ui-secret
type: Opaque
stringData:
  # Placeholder value — do not commit real secrets. Create this secret with kubectl instead.
  VNC_PASSWORD: "REPLACE_ME"
