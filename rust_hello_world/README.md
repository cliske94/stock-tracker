Rust version of the HelloWorld stocks fetcher.

Usage:

```bash
cargo run --release
```

This will fetch the NASDAQ screener and print the top 5 results. A raw response is written to `/tmp/rust_hello_world_response.json` for debugging.
