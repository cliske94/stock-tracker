# Example values. Copy to .env and fill real values (do NOT commit .env to git).
OAUTH_PROVIDER=github
OAUTH_CLIENT_ID=REPLACE_ME
OAUTH_CLIENT_SECRET=REPLACE_ME
# Optional: force which browser command to use (e.g., firefox)
#BROWSER=firefox

# VNC password for local dev noVNC/x11vnc
VNC_PASSWORD=REPLACE_ME
